specVersion: "v1"
serviceId: "autopost"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "autopost-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
